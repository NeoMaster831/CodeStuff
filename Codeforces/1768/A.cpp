#include <bits/stdc++.h>

using namespace std;

void solve() {
    
    int K; cin >> K;
    cout << K - 1 << '\n';

    // Edge case: k = 2 (1! + 0! = 2, nothing happened)
}

int main() {
    cin.tie(0); cout.tie(0);
    ios_base::sync_with_stdio(0);

    int T; cin >> T;
    while (T--) solve();
}