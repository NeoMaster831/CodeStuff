#include <bits/stdc++.h>

using namespace std;
#define int long long

// Trivia Question.
// FIrst, we can divide segments by ceil(N / K).
// We can just put some stuffs

int N, M, K;

void solve() {
    
    cin >> N >> M >> K;

}

signed main() {
    cin.tie(0); cout.tie(0);
    ios_base::sync_with_stdio(0);
    int T; cin >> T;
    while (T--) solve();
}