#include <bits/stdc++.h>

using namespace std;
#define int long long

int N, K, D, W, T[200001];

void solve() {

    cin >> N >> K >> D >> W;
    for (int i = 1; i <= N; i++) cin >> T[i];

    // 1~4
    queue<int> q;
}

signed main() {
    cin.tie(0); cout.tie(0);
    ios_base::sync_with_stdio(0);
    int T; cin >> T;
    while (T--) solve();
}