#include <bits/stdc++.h>

using namespace std;
#define int long long

int N, M, K;

signed main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    cin >> N >> M >> K;
    for (int i = 0; i < K; i++) {
        
    }
}